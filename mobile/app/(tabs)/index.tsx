import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const Chat = () => {
  return (
    <ScrollView className='bg-surface-dark'>
      <Text>Hello form chat</Text>
    </ScrollView>
  )
}

export default Chat